# api-gateway-rate-limiter
One gateway to protect all your backend services. Provides JWT auth, distributed rate limiting (token bucket + Redis), IP throttling, request logging to PostgreSQL, and an analytics dashboard. Designed as a single entry point for multiple applications.
